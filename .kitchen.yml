---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - role[supply_chain_utility]
      - recipe[ohai]
      - recipe[nginx::source]
      - recipe[apps-nginx::default]
    attributes:
